# Changelog

## 0.4.3 — 2026-09-17 — сообщения пользователя находятся сами, тема в шапке видна

### Письмо пользователя не переводилось
- Причина (отчёт диагностики): «Сообщение» указывало только на один вид сообщений (`content-part-content` — ответы/бот), а письмо пользователя размечено иначе (`user-email-content`). Найдено было только английское письмо бота (`agent: 1, user: 0`).
- Теперь внутри каждого «Сообщения пользователя», где «Сообщение» ничего не нашло, расширение **само находит тело сообщения**: потомок с `data-testid`/классом со словами content / body / message / text / email-html (служебные — actions, pill, header, meta, avatar, timestamp, footer, toolbar — исключаются), самый внутренний, содержащий основной текст. Шапка письма (имя, адрес, «to:») и кнопки в перевод не попадают.
- Проверено на разметке из отчёта: письмо пользователя (португальский) найдено и переведено, письмо бота осталось сообщением агента.

### Тема в шапке переведена, но не видна
- Причина: сайт перерисовывает шапку и снимает класс, которым прятался оригинал; перевод оказывался после оригинала в обрезанной строке (за «…»).
- Теперь перевод темы ставится **в начало строки** (`🌐 перевод · оригинал`), оригинал не прячется — перевод виден всегда, даже в обрезанной шапке. Полный перевод и оригинал — во всплывающей подсказке.
- **Если сайт удалил перевод** (перерисовал элемент с тем же текстом) — расширение замечает удаление и показывает перевод заново. Это же работает и для подписей переводов сообщений и кнопок «🌐 перевести».

## 0.4.2 — 2026-09-17 — язык ответа из текущего тикета, панель не выходит за экран

### Язык ответа брался из предыдущего тикета
- Причина: если в тикете не находились переведённые сообщения пользователя (например, «Сообщение» настроено только на входящие письма), панель подставляла «последний язык на сайте» (`lastOutgoingLang`) или язык, найденный на вкладке раньше, — то есть язык прошлого тикета (немецкий чат → испанский).
- Теперь язык ответа определяется **только по текущему тикету**:
  1. 📌 закреплённый; 2. переведённые сообщения пользователя; 3. письмо во фрейме;
  4. **определение по тексту сообщений пользователя на странице** (по «Сообщение пользователя», без облака — встроенным детектором Chrome), даже если «Сообщение» настроено неточно;
  5. язык, который оператор уже выбирал **в этом тикете**;
  6. «Язык по умолчанию» из настроек.
- «Последний язык на сайте» больше не используется; вместо него — память языка по тикету (`ticketLangs`, 500 последних тикетов).
- Язык, найденный на вкладке, привязан к тикету и не переносится в другой.
- При открытии тикета язык определяется заранее (через 2,5 с) — защита отправки и кнопка «Перевести → DE» сразу знают язык нового тикета.
- Проверено на тестовом чате с неработающим «Сообщение»: испанский тикет → `es`, немецкий → `de`, турецкий → `tr`, снова испанский → `es`.

### Панель перевода выходила за экран на длинных сообщениях
- Высота панели ограничивается свободным местом над полем ввода (или под ним, или весь экран по центру, если места мало); шапка с выбором языка и кнопка «Вставить перевод» всегда видны.
- Длинный текст прокручивается внутри: поля оригинала/перевода — до 38% высоты экрана, обратный перевод — до 30%, общий блок — прокрутка.
- Панель пересчитывает положение при изменении размера окна.
- Проверено: 14 абзацев текста — панель целиком в экране, шапка и кнопка видны.

## 0.4.1 — 2026-09-17 — сначала скачанный переводчик, потом облако — для всего

Проблема (тест на реальном хелпдеске): при блокировке бесплатного Google перевод не работал вообще, хотя языковые пакеты скачаны. Причины:
1. Перевод ответа в панели всегда шёл в облако (встроенный переводчик был только для входящих и обратного перевода).
2. Встроенному переводчику нужен известный язык оригинала, а в панели его определял Google.
3. Скрипт на странице сайта работает в отдельном окружении, где Translator API может отсутствовать, — хотя на странице настроек (страница расширения), где скачивались пакеты, он есть.
4. Пакеты скачивались только в одну сторону (язык → английский), а для ответа нужны пары «язык оператора → язык пользователя».

Исправлено:
- **Новый режим по умолчанию «Для всего»** (`localTranslatorMode: 'all'`): скачанный переводчик Chrome используется первым для входящих, перевода ответа, обратного перевода и определения языка; облако — только если перевести на устройстве нельзя. Ключ настройки новый, поэтому режим включится у всех, кто сохранял настройки в 0.4.0.
- **Язык определяется без облака**: CLD Chrome → встроенный Language Detector (уверенность ≥ 0,7); иначе неуверенная догадка (≥ 0,3), а в панели ответа — по письменности (кириллица → язык операторов, латиница → английский).
- **Если облако недоступно** (лимит Google и т.п.) — повторная попытка на устройстве по неуверенной догадке о языке: входящие и ответ переводятся даже при заблокированном Google.
- **Мост через страницу расширения**: если на странице сайта Translator API недоступен или не отвечает, перевод и определение языка выполняются на скрытой странице расширения (offscreen document, новое разрешение `offscreen`), где API доступен. Выбор автоматический; в диагностике `localTranslator.backend` показывает, где выполнен последний перевод (`page` / `bridge`), `localTranslator.bridge` — есть ли API на странице расширения.
- **Защита отправки** определяет язык текста сначала без облака; если облако недоступно — использует догадку, а не показывает «другой язык».
- **Скачивание пакетов в обе стороны**: для каждого языка собеседника — пары с языком перевода входящих, языками операторов и языком обратного перевода, в обе стороны (например, es→en, en→es, es→ru, ru→es).
- Таймауты встроенного переводчика увеличены (проверка 5 с, создание 15 с, перевод 20 с); отключение API на странице — после 5 зависаний (дальше работает мост).

Проверено на dev-стенде (эмуляция встроенного переводчика, все запросы к Google возвращают 429):
- входящие испанские: уверенный язык — на устройстве; неуверенный — одна попытка облака (сразу отказ) и перевод на устройстве по догадке;
- панель ответа, русский текст: «С: Русский», перевод ru→es и обратный es→ru на устройстве, **0 запросов к Google**;
- панель ответа, английский текст (как на скриншоте): перевод en→es на устройстве.
Не проверено вживую: мост через offscreen document в установленном расширении.

## 0.4.0 — 2026-09-17 — меньше запросов к Google, перевод на устройстве, резерв Google Cloud

Причина: бесплатный Google несколько раз за день ограничивал IP (перенаправление на страницу проверки) даже при ~170 запросах.

### 1. Меньше запросов
- **Перевод только видимых сообщений** (`onlyVisible`, по умолчанию вкл). Сообщение переводится, когда оно на экране или рядом (±600px) — через IntersectionObserver (работает и в Shadow DOM). История тикета выше экрана не переводится, пока до неё не долистали. Проверено: из 43 сообщений переведены только видимые 3, после прокрутки вниз — ещё 4 появившихся.
- **Объединение запросов** (`batchRequests`, по умолчанию вкл). Сообщения одного языка (по встроенному детектору Chrome), пришедшие в течение 150 мс, уходят одним запросом (до 20 сообщений / 3500 символов):
  - бесплатный Google — тексты склеиваются через метки `⟦900001⟧`; если после перевода частей не столько же — сообщения автоматически переводятся по одному;
  - Google Cloud — нативный массив `q`, язык определяется для каждого текста.
  - Проверено на подменённом ответе: 3 сообщения → 1 запрос; при «съеденных» разделителях — корректный перевод по одному.
- **Постоянный кэш переводов** в `storage.local` (раньше — до перезапуска браузера): до 3000 записей, срок 30 дней, старые удаляются автоматически. Повторно открытые тикеты не переводятся заново. «Очистить кэш» чистит и его.
- Статистика: «переведено на устройстве», «сообщений объединено».

### 2. Перевод на устройстве (встроенный переводчик Chrome)
- Новый модуль `content/local-translator.js` — Translator API и Language Detector API (Chrome 138+): перевод прямо в браузере, **без сети, без лимитов, тексты не уходят в Google**.
- Настройка **«Перевод на устройстве»** (`localTranslator`): для входящих и обратного перевода (по умолчанию) / только входящие / не использовать. Перевод ответа пользователю всегда идёт в облаке (качество важнее).
- Язык источника: встроенный детектор Chrome (CLD), если уверен; иначе Language Detector API (уверенность ≥ 0,7). Без уверенного языка — облако.
- Используются только **уже скачанные** языковые пакеты (скачивание требует клика). Настройки → «Перевод на устройстве» → **«Проверить пакеты»** / **«Скачать языковые пакеты»** для списка языков собеседников.
- Глоссарий работает: если метки терминов не пережили перевод на устройстве — сообщение переводится в облаке.
- **Защита от зависания API**: таймауты (проверка пакета 3 с, создание 10 с, перевод 15 с) → облако; после 3 зависаний встроенный переводчик отключается до перезагрузки страницы. Проверено: при не отвечающем API сообщение переведено облаком за 3,3 с.
- Подпись перевода показывает источник: «на устройстве (Chrome)», «Google», «Google Cloud».
- Диагностика: `found.localTranslator` (наличие API, недостающие пакеты), `onlyVisible`.

### 3. Резерв Google Cloud
- При ограничении/недоступности бесплатного Google (в т.ч. во время паузы предохранителя) запросы **сразу идут в Google Cloud** — и одиночные, и объединённые. Проверено на подменённых ответах: бесплатный 429 → оба сообщения в Cloud одним запросом; следующий запрос во время паузы — сразу в Cloud, бесплатный не запрашивается.
- Настройки: подписи «Google Cloud API key (резерв)», «Резерв: …», инструкция по созданию ключа, кнопка **«Проверить резерв Google Cloud»** (запрос именно в Cloud, без кэша, с понятной ошибкой: ключ / API не включён / нет оплаты).
- Диагностика: `cloudFallback` (резерв настроен), `translator.cooldown` (какой провайдер на паузе и сколько секунд).

### Исправлено
- **Тема письма в шапке не отображалась**: шапка — однострочный элемент с фиксированной высотой и обрезкой, а перевод добавлялся блоком под текстом и обрезался (виднелась только полоска). Теперь перевод темы показывается **на месте оригинала в той же строке**; оригинал — во всплывающей подсказке, клик по переводу на 4 секунды показывает оригинал. Проверено на шапке высотой 20px с `overflow:hidden`.
- Смена «Перевод на устройстве» и «Только видимые» в настройках сразу перестраивает переводы.

### Изменённые файлы
- Новые: `extension/content/local-translator.js`.
- `extension/lib/translator.js` — переписан: постоянный кэш, объединение запросов, цепочка провайдеров с резервом, `testProvider`/`status`.
- `extension/lib/providers.js` — `translateMany` (Google бесплатный через разделители, Google Cloud массивом, тестовый).
- `extension/lib/stats.js`, `extension/stats/stats.js` — счётчики «на устройстве» и «объединено».
- `extension/lib/defaults.js` — `localTranslator`, `onlyVisible`, `batchRequests`.
- `extension/content/core.js` — маршрутизация: на устройстве → облако.
- `extension/content/incoming.js` — видимость, язык источника для встроенного переводчика, объединение, тема на месте оригинала, подписи источника.
- `extension/content/outgoing.js` — обратный перевод на устройстве.
- `extension/content/styles.js` — стиль темы на месте оригинала.
- `extension/content/main.js`, `diag.js` — новые настройки, диагностика.
- `extension/background.js` — новые скрипты страницы, очистка постоянного кэша, `testProvider`, `providerStatus`, диагностика резерва.
- `extension/options/*` — разделы «Перевод на устройстве», резерв Google Cloud.
- `test-site/harness.js` — `?fakelocal=1` (эмуляция встроенного переводчика).

## 0.3.1 — 2026-09-17 — по итогам теста на реальном хелпдеске

### Сообщения агентов — перевод по кнопке
- Новое поле правила **«Сообщения агентов»** (`agentMessages`): **по кнопке** (по умолчанию) / автоматически / не переводить.
- Агент = сообщение, которое не попадает под «Сообщение пользователя» (`userMessageSelector`). Без этого селектора все сообщения считаются сообщениями пользователя (как раньше).
- В режиме «по кнопке» у сообщения агента — маленькая кнопка **«🌐 перевести»**; запрос к переводчику уходит только после клика. Английские сообщения (по встроенному детектору Chrome) кнопку не получают.
- Запрошенный перевод держится при обновлениях ленты (запоминается по хэшу текста); если текст сообщения изменился — снова кнопка.
- Переводы сообщений агентов не считаются во «входящих» статистики и не влияют на язык собеседника.
- Диагностика: `messageBreakdown` (всего / пользователь / агент), `agentButtons`.

### Пикер «Пользователь vs оператор» понимает «сторону» сообщения
- Если части переписки одинаковые, а сообщения агента отличаются классом выравнивания (`justify-end`, `self-end`, `ml-auto`, `out`…), пикер строит селектор вида **`[общий признак]:not(.класс-стороны)`** — он охватывает и письма, и чат-сообщения пользователя. Проверено на разметке, повторяющей реальный хелпдеск: получен `[data-testid^="conversation-part-id-"]:not(.justify-end)`.
- Атрибуты с номером в конце (`conversation-part-id-464826615`) превращаются в «начинается с» (`[data-testid^="conversation-part-id-"]`) — во всех пикерах.

### «Окно чата» слишком общее
- Причина: пикер строил путь из безликих `div > div > …` (783 совпадения) — тяжёлый обход и дубли сообщений (36 вместо 2).
- Пикер «1. Окно чата» теперь сначала ищет смыслового предка (id, класс, `data-testid`, `role`, `aria-label`), совпадающего не более чем с 3 элементами.
- Если окно всё же совпало больше чем с 30 элементами — сообщения ищутся одним проходом по странице, а самопроверка показывает предупреждение «Окно чата выбрано слишком общо».
- Найденные сообщения без дублей; при вложенных совпадениях берётся самый внутренний элемент.

### Режим заметки — без ложных срабатываний
- В отчёте был `noteModeNow: true` при режиме Email — защита отправки фактически не работала.
- Подпись режима считается активной, только если: выбранная вкладка/переключатель (`aria-selected/pressed/checked=true`, `data-state=active`, класс `active/selected` — у самого элемента или прямого родителя), **или** это кнопка-выпадашка (`aria-haspopup`), на которой написан режим. Пункты меню (`role=menu/menuitem/option`) и обычные кнопки «Note» больше не считаются.
- Диагностика: `noteModeReason` — какой элемент и почему решил, что это заметка.

### Ошибка в «Расширения → Ошибки» (CORS, google.com/sorry)
- Когда Google ограничивает IP, он перенаправляет запрос на страницу проверки `google.com/sorry`; fetch шёл по редиректу и Chrome записывал CORS-ошибку в ошибки расширения, а запрос повторялся 3 раза.
- Теперь `redirect: 'manual'`: перенаправление распознаётся сразу как ограничение (429) → пауза предохранителя, без повторов и без записи CORS-ошибки.
- Проверка орфографии (`dt=qca`) запрашивается **только для текста оператора** в панели ответа (входящие — только перевод): меньше «тяжёлых» запросов; ключ кэша учитывает этот флаг.

### Прочее
- Текст сообщения больше не включает служебные элементы: кнопки (`Reply to this message`), `role=button`, `aria-hidden`, SVG.
- Крошечные служебные фреймы (виджет мессенджера 1×1, кнопка 48×48) не обрабатываются и не участвуют в самопроверке.
- Смена «Сообщение пользователя» или «Сообщения агентов» в настройках сразу перестраивает переводы.

## 0.3.0 — 2026-09-17 — пункты 1–6 и 9–12 из плана

### 1. Внутренние заметки не проверяются и не переводятся
- Композер хелпдеска переключается между «Reply» и «Note». В режиме заметки **защита отправки не срабатывает**, **кнопка «🌐 Перевести» скрывается**.
- Режим определяется эвристикой (`ST.isNoteMode`, `content/core.js`): от поля ввода поднимаемся к «шапке» композера и ищем видимую подпись активного режима (`Note`, `Internal note`, `Заметка`, `Внутренняя заметка`) у элемента-индикатора: кнопка-выпадашка (`aria-haspopup`/`aria-expanded`), активная вкладка (`aria-selected`, `aria-pressed`, `data-state=active`, класс `active/selected/current`).
- Новые поля правила сайта: **«Режим заметки: подписи активного режима»** (`noteModeText`, список через запятую) и **«Режим заметки: селектор»** (`noteSelector`) — явный селектор на случай, если эвристика не срабатывает.
- Кнопка перевода перепроверяет режим после любого клика (переключение Reply/Note).

### 2. Сохранение абзацев при вставке перевода
- `ST.getFieldText` для редакторов с абзацами (`<p>/<div>` на строку — ProseMirror, Draft и т.п.) собирает текст построчно; раньше `innerText` вставлял лишние пустые строки между абзацами.
- `ST.setFieldText` вставляет перевод **построчно**: `insertText` + `insertParagraph` между строками — как если бы оператор печатал и нажимал Enter; редакторы видят обычный ввод, работает Ctrl+Z. Если редактор не принял ввод — запасной вариант собирает `<p>` на каждую строку.
- Пустые строки (разрывы между абзацами) сохраняются.

### 3. Проверка чисел, сумм, валют, телефонов, ссылок и почт
- Новый модуль `lib/checks.js` → `compareFacts(оригинал, перевод)`.
- Сравниваются: числа (с нормализацией форматов: `34,99` = `34.99`, `1 234,50` = `1,234.50`, `1.234.567` = `1,234,567`), даты/время/версии по частям (`17.09.2026` = `17/09/2026` = `2026-09-17`), телефоны по цифрам (`+7 999 123-45-67` = `+7 999 123 45 67`), валюты (`USD`, `EUR`… и символы `$ € £ ₽ ₺ ¥ ₹ ₴ ₸`), ссылки, почты (без учёта регистра).
- В панели ответа: красная плашка **«🔢 Проверьте числа и ссылки. Нет в переводе: … Появилось в переводе: …»**, кнопка меняется на **«⚠ Вставить всё равно»**. Проверка повторяется при ручной правке перевода.
- Настройка: **«Сверять числа, суммы, валюты, ссылки и почты»** (`checkNumbers`, по умолчанию вкл).

### 4. Тёмная тема панелей
- Панель перевода и предупреждение защиты используют CSS-переменные темы; тёмная тема включается **автоматически по фону сайта** вокруг поля ввода (`ST.isDarkAround` — яркость первого непрозрачного фона среди предков, включая хосты shadow root).
- Настройка **«Оформление панелей расширения»** (`uiTheme`): Авто / Светлое / Тёмное.
- Подписи переводов во входящих подстроены под тёмный фон (цвета наследуются, акцент светлее).

### 5. Язык собеседника из письма во фрейме
- Когда во встроенном фрейме (HTML-письмо) переведено сообщение пользователя, фрейм сообщает язык фоновому скрипту (`convLang`), тот рассылает его остальным фреймам вкладки (`frameLang`).
- `ST.expectedLang()`: 📌 закреплённый → последнее сообщение пользователя в своём фрейме → **язык из другого фрейма** (источник `frame`, в popup — «(из письма)»). Язык из фрейма сбрасывается при смене тикета (смене URL).
- Сообщения операторов (по `userMessageSelector` или `operatorLangs`) язык не передают.

### 6. Самопроверка настроек
- Каждые 5 с (и после смены настроек) каждый фрейм оценивает своё состояние (`ST.incoming.health()`):
  - **ошибка**: окно чата найдено и в нём есть текст, но сообщения по селектору не находятся — «вероятно, сайт обновился»;
  - **предупреждение**: не выбрано «Окно чата»; поле ввода по селектору не найдено при наличии полей на странице; Google ограничил запросы; есть ошибки перевода на странице.
- Фоновый скрипт сводит состояние всех фреймов вкладки: **значок на иконке** расширения (`!` красный — ошибка, `•` оранжевый — предупреждение), текст проблем — во всплывающей подсказке иконки и списком в popup.
- Диагностика дополнена полем `health`.

### 9. Метки языка в списке тикетов
- Новое поле правила **«Превью в списке тикетов»** (`listItemSelector`) и пикер **«7. Превью в списке тикетов»**.
- Перед текстом превью ставится метка языка (`ES`, `TR`, `PT`…) для иностранных тикетов; английские и языки из «Не переводить» не помечаются.
- По умолчанию язык определяется **только встроенным детектором Chrome — без запросов в сеть**. Настройка **«Для коротких превью уточнять язык у переводчика»** (`listBadgesUseNetwork`, по умолчанию выкл) — точнее, но тратит запросы.
- Общий выключатель **«Метки языка в списке тикетов»** (`listBadges`).

### 10. Перевод темы письма
- Новое поле правила **«Тема письма/тикета»** (`subjectSelector`) и пикер **«8. Тема письма»**.
- Под темой — компактная строка `🌐 перевод` (без кнопок, подробности — во всплывающей подсказке). В статистику входящих не считается.

### 11. Вежливость обращения («ты» / «вы»)
- Google не даёт выбрать форму обращения, поэтому сделано через проверку: `informalMarkers(перевод, язык)` ищет обращение на «ты» для es, pt, de, fr, it, tr, nl, pl (`tú/puedes`, `du/dein`, `tu/t'`, `sen/sana`…).
- В панели: плашка **«🤝 Перевод обращается на «ты»: …»**. Если оригинал на русском и содержит «вы/ваш» со строчной — кнопка **«Перевести вежливо («Вы»)»**: делает «Вы/Вас/Ваш…» заглавными (подсказка переводчику на вежливую форму) и переводит заново. Если не помогло — совет поправить вручную или через глоссарий.
- Настройка **«Вежливость обращения»** (`formality`): предупреждать / не проверять.
- ⚠️ Насколько заглавное «Вы» меняет выбор Google, проверить на живом API не удалось (Google ограничил запросы с тестового IP) — сама проверка работает независимо от этого.

### 12. Подсказка опечаток
- Бесплатный провайдер запрашивает у Google проверку орфографии исходного текста (`dt=qca`); исправленный вариант возвращается в результате перевода (`spell`), учитывая метки глоссария.
- В панели: плашка **«✏️ Возможно, опечатка. Вариант: «…»»** с кнопкой **«Исправить и перевести»**.
- Настройка **«Подсказывать исправление опечаток»** (`spellCheck`). У официального Cloud API подсказок нет — плашка просто не появляется.
- ⚠️ Формат ответа Google с `dt=qca` на живом API не перепроверен по той же причине; если поле не придёт — функция молча не показывается.

### Прочее
- **Тестовый провайдер «Тестовый — без сети»** (`dev-mock`): переводит как `[es] текст`, определяет язык по ключевым словам, знает несколько опечаток — для проверки интерфейса без расхода лимитов Google. В dev-стенде: `?mock=1`.
- **Ключ кэша переводов включает провайдера** — тестовые переводы не попадают в кэш настоящего.
- **Страница «Sorry… / captcha» от Google** распознаётся как ограничение (429) — срабатывает предохранитель, а не «непонятная ошибка».
- Диагностика: темы, превью, текущий режим заметки, тёмная тема, самопроверка.
- Тестовый чат: тема письма (`#chatSubject`), переключатель **Reply / Note** (`#mode`), тёмная тема `?dark=1`; в dev-стенде `stHarness.message(...)` для эмуляции сообщений фоновой части.

### Новые настройки (сводка)
| Где | Поле | По умолчанию |
|---|---|---|
| Общие | `uiTheme` — оформление панелей | `auto` |
| Общие | `checkNumbers` — сверка чисел/ссылок | `true` |
| Общие | `formality` — вежливость обращения | `formal` |
| Общие | `spellCheck` — подсказка опечаток | `true` |
| Общие | `listBadges` — метки языка в списке | `true` |
| Общие | `listBadgesUseNetwork` — уточнять язык превью у переводчика | `false` |
| Правило сайта | `noteModeText` — подписи режима заметки | `Note, Internal note, Заметка, Внутренняя заметка` |
| Правило сайта | `noteSelector` — селектор режима заметки | пусто |
| Правило сайта | `listItemSelector` — превью в списке | пусто |
| Правило сайта | `subjectSelector` — тема письма | пусто |

### Изменённые файлы
- Новые: `extension/lib/checks.js`.
- `extension/lib/defaults.js` — новые настройки и поля правила; тестовое правило дополнено `listItemSelector`, `subjectSelector`.
- `extension/lib/providers.js` — `dt=qca` и `spell`, распознавание страницы captcha, провайдер `dev-mock`.
- `extension/lib/translator.js` — передача `spell`, провайдер в ключе кэша.
- `extension/lib/glossary.js` — `originals` для восстановления исходного текста в подсказке опечаток.
- `extension/content/core.js` — режим заметки, тема, язык из фрейма, построчная вставка/чтение поля, последняя ошибка перевода.
- `extension/content/incoming.js` — тема письма, метки языка в списке, передача языка другим фреймам, самопроверка, порог «сообщения не найдены».
- `extension/content/outgoing.js` — тема, блок проверок (опечатки, числа, вежливость), скрытие кнопки в заметке.
- `extension/content/guard.js` — пропуск заметок, тёмная тема.
- `extension/content/styles.js` — стили компактного перевода и меток языка.
- `extension/content/main.js` — приём `frameLang`, сброс при смене тикета, отправка самопроверки, `health` в статусе.
- `extension/content/picker.js` — пикеры «Превью в списке», «Тема письма».
- `extension/content/diag.js` — новые поля отчёта.
- `extension/background.js` — `lib/checks.js` в скриптах страницы, пересылка `convLang` → `frameLang`, значок самопроверки, `health` в диагностике.
- `extension/popup/*` — кнопки 7–8, список проблем самопроверки, «(из письма)».
- `extension/options/*` — разделы «Проверки перевода ответа», «Интерфейс», новые поля правила, тестовый провайдер.
- `test-site/mock-chat.html`, `test-site/harness.js` — тема, Reply/Note, `?dark=1`, `?mock=1`, `stHarness.message`.

## 0.2.2 — 2026-09-17 — поддержка Shadow DOM

Причина (по отчёту диагностики с реального сайта): интерфейс хелпдеска целиком рисуется внутри открытых Shadow DOM — обычный поиск элементов, наблюдение за изменениями, пикеры и стили туда не проникали; при пустых селекторах переводились служебные надписи всей страницы.

Исправлено / добавлено:
- **Shadow DOM везде**: поиск окон и сообщений, наблюдение за новыми сообщениями, поле ввода и кнопка отправки (защита), ID тикета, язык собеседника, пикеры (выбор элемента под курсором внутри shadow root), диагностика (путь с границей `>>`).
- **Стили подписей подключаются внутрь shadow root** (`content/styles.js`, constructable stylesheets); `content.css` удалён.
- **Без выбранного «Окна чата» перевод не запускается** — больше не переводится весь интерфейс сайта.
- **Пикер «6. Сообщение пользователя vs оператора»**: клик по сообщению пользователя, затем по сообщению оператора — расширение само находит отличающий класс/атрибут.
- Пикер сообщения поднимается от `<p>/<span>` к ближайшему элементу со смысловым классом; утилитарные классы (Tailwind: `flex`, `h-full`, `p-4`…) не попадают в селекторы.
- **Предохранитель лимитов Google**: при 429 или 3 неудачах подряд — пауза 60 с без запросов (или сразу резервный Cloud API, если задан ключ); плашки ошибок сами повторяют перевод после паузы.
- Наблюдение за DOM — троттлинг вместо debounce (на «живых» страницах debounce мог не срабатывать).
- Диагностика: кандидаты в поле ввода и кнопку отправки, число shadow root.
- Тестовый чат: режим `?shadow=1` (весь интерфейс в Shadow DOM).

## 0.2.1 — 2026-09-17 — первые правки после теста на реальном сайте

Исправлено:
- Встроенные фреймы без своего адреса (`about:srcdoc`, `about:blank`) — в них хелпдески показывают HTML-письма — не сопоставлялись с правилом сайта. Теперь используется адрес родительской страницы, а если селекторов окна во фрейме нет, весь документ фрейма считается одним сообщением.
- Короткие надписи интерфейса (< 40 символов), которые Chrome считает английскими, больше не отправляются в переводчик — лишние запросы на английском UI.

Добавлено:
- Popup → **🩺 Диагностика**: собирает по всем фреймам вкладки правило, найденные элементы, iframe, Shadow DOM, крупные текстовые блоки (только структура и длины, без текста переписки) и копирует отчёт в буфер.
- Тестовый чат: тикет #6 — письмо внутри `iframe srcdoc`.

## 0.2.0 — 2026-09-17 — готовность к пилоту

Добавлено:
- **Защита от отправки непереведённого текста.** Перехват Enter / Ctrl+Enter и клика по кнопке отправки. Мгновенная проверка по письменности (кириллица ↔ латиница и т.д.), для одинаковой письменности — определение языка (CLD → Google). Предупреждение: «Перевести» / «Отправить как есть» / «Отмена». Вставленный через панель перевод и короткие сообщения («ok», «спасибо») проходят без проверки.
- **Глоссарий**: термины «не переводить» и фиксированные переводы по языкам (`Кошелёк => ru: Кошелёк | es: Billetera`). Работает для входящих, исходящих и обратного перевода; проверка прямо в настройках.
- **Закрепление языка для тикета** (📌 в панели перевода). Тикет определяется по селектору ID тикета или по URL.
- **Язык собеседника только из его сообщений**: селектор «Сообщение пользователя» или список языков операторов (`operatorLangs`, по умолчанию `ru`).
- **Поддержка iframe**: скрипты работают во всех фреймах вкладки; команды popup и горячие клавиши доходят до нужного фрейма; выбор элементов работает внутри фрейма.
- **Доступ только к подключённым сайтам**: вместо `<all_urls>` — `optional_host_permissions` и динамическая регистрация скриптов. Кнопка «Подключить этот сайт» в popup, статус доступа и «Выдать доступ» у каждого правила в настройках.
- **Режим вставки «после сообщения»** — для сайтов, чей фреймворк не терпит чужих узлов внутри сообщений; автоудаление «осиротевших» подписей.
- **Статистика (из плана v0.3)**: история по дням (90 дней) — входящие/ответы по языкам, символы и запросы, кэш, ошибки, блокировки отправки. Страница статистики: плитки, график по дням, языки, оценка стоимости официального API, таблица, экспорт JSON/CSV, объединение файлов коллег для командных цифр.
- Popup: подключение сайта, статус по всем фреймам (язык собеседника, 📌, защита), пикеры «Кнопка отправки» и «ID тикета», ссылка на статистику.
- Настройки: новые поля правил, языки операторов, порог защиты, глоссарий, Ctrl+S, предупреждение о несохранённых изменениях.
- После обновления расширения старые скрипты на открытых вкладках сразу отключаются и просят обновить страницу.
- Тестовые страницы: `mock-iframe.html` (чат во фрейме с другого origin), `stats-preview.html` (статистика на демо-данных).

Изменено:
- Статистика хранится в `statsDays` (старый ключ `stats` 0.1.0 удаляется при обновлении).
- Кэш перевода учитывает версию глоссария.
- Тост после вставки перевода — вверху экрана (не перекрывает поле ввода).

Миграция с 0.1.0:
- После обновления расширение **не работает ни на одном сайте, пока не выдан доступ**: popup → «Подключить этот сайт» или настройки → «Выдать доступ» у правила.

## 0.1.0 — 2026-09-17 — прототип

Добавлено:
- Автоперевод входящих сообщений в заданных окнах (MutationObserver, правила сайтов по URL + CSS-селекторам).
- Подпись «🌐 Переведено: язык → язык», режимы «под оригиналом» / «вместо оригинала», копирование перевода.
- Локальное определение языка (встроенный в Chrome CLD) — английский не отправляется в переводчик.
- Перевод исходящего текста через панель подтверждения: оригинал / редактируемый перевод / обратный перевод с подсветкой расхождений; вставка только по подтверждению; «Вернуть оригинал».
- Автовыбор языка ответа по последнему иностранному сообщению в текущем окне.
- Поддержка textarea, input и contenteditable (совместимо с React/Vue за счёт insertText + input-событий).
- Провайдеры: Google free (translate.googleapis.com, gtx) и Google Cloud Translation v2 (ключ) с автофолбэком.
- Очередь (4 параллельных запроса), ретраи 429/5xx с экспоненциальной задержкой, дедупликация, кэш (память + storage.session).
- Popup: статус сайта, настройка окон кликом по странице, статистика за день.
- Страница настроек: языки, режим показа, провайдер, правила сайтов, импорт/экспорт.
- Горячие клавиши: Ctrl+Shift+Y — перевести ввод, Ctrl+Shift+U — перевести окно заново.
- Тестовый чат `test-site/mock-chat.html` и dev-стенд `?harness=1`.
